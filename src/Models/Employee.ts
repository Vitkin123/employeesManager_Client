export interface Employee {
    id: string,
    name?: string,
    lastName?: string,
    role?: string,
    password?: string,
    email?: string,
    birthDate?: Date,
    startWorkingDate?: Date,
    salary?: number,
    position?: number,
    monthsOfExperience?: number
}